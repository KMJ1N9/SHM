<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shm.admin.mapper.UserMapper">

    <!-- 管理端用户列表（搜索 + 筛选 + 分页，与 Node.js userRepo.listWithFilters 一致） -->
    <select id="listWithFilters" resultType="com.shm.common.model.entity.User">
        SELECT id, phone, nickname, avatar, class_name, dorm_building, role, status,
               token_version, credit_score, created_at, updated_at
        FROM users
        <where>
            <if test="keyword != null and keyword != ''">
                AND (nickname LIKE CONCAT('%', #{keyword}, '%') OR phone LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="role != null and role != ''">
                AND role = #{role}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 管理端用户计数 -->
    <select id="countWithFilters" resultType="long">
        SELECT COUNT(*) FROM users
        <where>
            <if test="keyword != null and keyword != ''">
                AND (nickname LIKE CONCAT('%', #{keyword}, '%') OR phone LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="role != null and role != ''">
                AND role = #{role}
            </if>
        </where>
    </select>

</mapper>
